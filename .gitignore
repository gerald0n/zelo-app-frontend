# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem
*:Zone.Identifier

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
*.log

# env files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
!.env.example


# Evolution API — sessão/credenciais se usar bind mount local
/evolution-data/

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

certificates

# Ferramentas de IA (instaladas localmente via `npx skills` / `impeccable` /
# codex — não são código do projeto). skills-lock.json fica versionado: é só o
# manifesto para reinstalar as skills.
/.claude/skills/
/.claude/settings.local.json
/.agents/
/.impeccable/
/.codex/
/.github/skills/
/.github/agents/
/.github/hooks/

# npm lockfile — o projeto usa pnpm
package-lock.json
.env*

# Seed operacional de produção: dados reais de um tenant (endereço, contato,
# Pix, coordenadas). Não vai pro repo. `seed-operacional.sql` (com placeholders
# <<<PREENCHA>>>) fica versionado como modelo.
/supabase/seed-operacional.prod.sql

# previews locais de comanda (scripts/preview-receipt.mjs)
receipt-preview-*.png
